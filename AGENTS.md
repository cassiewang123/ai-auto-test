# 项目开发规则

## Git 提交与推送

- 每完成一个独立需求、缺陷修复或重构任务，都必须先完成相应验证，再提交到本地 Git，并立即推送到当前分支对应的远程仓库。
- 每个提交只包含当前需求相关的代码、脚本、配置和文档，不得混入其他未完成或无关改动。
- 提交前必须检查 `git status`、`git diff` 和测试结果，避免提交构建产物、本地配置、日志、缓存、数据库文件或敏感信息。
- 使用清晰、可追踪的提交说明；不同需求应分别提交，禁止将多个无关需求合并为一个提交。
- 推送失败时必须保留本地提交并明确报告原因，不得将任务标记为已完成或声称已经推送成功。
